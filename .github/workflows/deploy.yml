name: Auto Deploy to OCI OKE

on:
  push:
    branches:
      - main   # deploys automatically on pushes to main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Deploy to OCI OKE
        run: |
          echo "${{ secrets.KUBECONFIG }}" > kubeconfig.yml
          export KUBECONFIG=$(pwd)/kubeconfig.yml
          kubectl apply -f k8/deployment.yml
          kubectl apply -f k8/service.yml

          
          if [ -f k8/ingress.yml ]; then
            kubectl apply -f k8/ingress.yml
          fi
